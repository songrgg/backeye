package dao

import (
	"testing"
)

func TestRun(t *testing.T) {
	// bytes, err := ioutil.ReadFile("normal_target.json")
	// assert.NoError(t, err)

	// v := model.Target{}
	// json.Unmarshal(bytes, &v)

	// err = NewTarget(&v)
	// assert.NoError(t, err)
}

func TestFind(t *testing.T) {
	// target, err := GetTarget("Post API")
	// assert.NoError(t, err)
	// assert.Equal(t, "Post API", target.Name)
}
